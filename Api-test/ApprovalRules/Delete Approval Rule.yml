info:
  name: Delete Approval Rule
  type: http
  seq: 4

http:
  method: DELETE
  url: http://localhost:3000/api/approval-rules/RULE_ID_HERE
  headers:
    Authorization: Bearer YOUR_ADMIN_TOKEN_HERE
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

tests:
  - name: Status code is 200
    assert: $res.status == 200
  - name: Response has success message
    assert: $res.body.message.includes("deleted")
