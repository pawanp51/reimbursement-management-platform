info:
  name: Create Admin (SignUp)
  type: http
  seq: 1

http:
  method: POST
  url: http:/localhost:3000/api/auth/signup
  body:
    type: json
    data: |-
      {
        "email":"pawanrp51@gmail.com",
        "password":"Test@123",
        "firstName":"Porn",
        "lastName":"Hub",
        "country":"India"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
