info:
  name: Verify Otp
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:3000/api/auth/verify-otp
  body:
    type: json
    data: |-
      {
        "email":"pranavp1608@gmail.com",
        "otp":"286362"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
